import random
dice1 = random.randint(1,6)
dice2 = random.randint(1,6)
print(f"the value of simulating first dice is {dice1}")
print(f"the value of simulating first dice is {dice2}")
total = dice1+dice2
print(f"the total of simulating both dices is {total}")
